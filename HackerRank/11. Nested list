> lamda function: Yes, exactly! A lambda function is a literal shortcut for a standard function, especially when you need a quick, one-line piece of logic without fully defining it with def.

> Problem:

if __name__ == '__main__':
    
    stud =[]
    for _ in range(int(input())):
        name = input()
        score = float(input())
        stud.append([name,score])
        
    score = sorted(set(s[1] for s in stud))
        
    sec_l = score[1]
        
    result = sorted(s[0] for s in stud if s[1]==sec_l)
        
    for i in result:
                   
         print(i)
        
        
